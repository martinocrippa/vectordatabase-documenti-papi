name: vectordb-papi
channels:
  - conda-forge
dependencies:
  - python=3.12
  - numpy>=1.24
  - py3langid>=0.3            # riconoscimento lingua dei chunk (da conda-forge)
  - pip
  - pip:
      # torch (cpu) arriva come dipendenza di sentence-transformers
      - sentence-transformers>=2.2
      - lancedb>=0.24          # store embedded + ricerca ibrida (vettori + full-text + RRF)
