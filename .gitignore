# Python
__pycache__/
*.pyc
.venv/
venv/

# Dati e risorse: LOCALI, non versionati (vedi note.txt).
# I dati raw si rigenerano col repo ingestion-documenti-papi.
data/
resources/
*.log

# Indice vettoriale generato: rigenerabile e contiene spezzoni dei testi
# (materiale sotto copyright). Locale, non versionato.
indice/

# Pesi del modello di embedding (cache Hugging Face, HF_HOME). Scaricati a
# richiesta, ~centinaia di MB. Locali, non versionati.
models/
